{
  "name": "ipdb-metarepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "husky": {
    "hooks": {}
  },
  "scripts": {
    "dev": "echo dev not set",
    "format": "pretty-quick",
    "ipdb": "yarn workspace ipdb",
    "build": "yarn workspaces run build",
    "publish": "yarn hook publish"
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "prettier": "^2.1.2"
  }
}
